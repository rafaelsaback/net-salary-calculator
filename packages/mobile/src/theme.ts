import { DefaultTheme } from '@react-navigation/native';

export const appTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#DC143C',
    text: '#515151',
    background: 'transparent',
  },
};
